# 2 "seqc.cyc"

void memcpy(char *@fat @nozeroterm dest, const char *@fat @nozeroterm src, unsigned long n) {
  unsigned long i;
  if (n > numelts(src) || n > numelts(dest))
    return;
  for (i = 0; i < n; i++) {
    dest[i] = src[i];
  }
}
